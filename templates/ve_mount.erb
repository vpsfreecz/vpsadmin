#!/usr/bin/env ruby
require '/opt/vpsadmind/lib/vpsadmind/standalone'

VEID = <%= @vps_id %>
MOUNTS = [
<%= @mounts.map do |mnt|
    PP.pp(mnt, '').strip
end.join(",\n") %>
]

puts "Executing #{$0}"
VpsAdmind::Mounter.mount_all(VEID, MOUNTS, false)

exit(0)
