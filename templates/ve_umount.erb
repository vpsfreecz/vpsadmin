#!/usr/bin/env ruby

ENV['CONFIG'] = "<%= $CFG.file %>"

require '/opt/vpsadmind/lib/vpsadmind/standalone'

VEID = <%= @vps_id %>
MOUNTS = [
<%= @mounts.map do |mnt|
    PP.pp(mnt, '').strip
end.join(",\n") %>
]

puts "Executing #{$0}"
VpsAdmind::Mounter.umount_all(VEID, MOUNTS)

exit(0)
