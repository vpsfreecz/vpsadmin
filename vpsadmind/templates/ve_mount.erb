#!/usr/bin/env ruby
ENV['CONFIG'] = "<%= $CFG.file %>"
require '/opt/vpsadmin/vpsadmind/lib/vpsadmind/standalone'

VEID = <%= @vps_id %>
load '<%= File.join($CFG.get(:vpsadmin, :mounts_dir), "#{@vps_id}.mounts") %>'

puts "Executing #{$0}"
VpsAdmind::Mounter.mount_all(VEID, MOUNTS, false, mode: :actionscript)

exit(0)
