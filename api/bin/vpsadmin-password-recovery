#!/usr/bin/env ruby
require_relative '../lib/vpsadmin'

$stdout.sync = true
$stderr.sync = true

worker = VpsAdmin::API::PasswordRecoveryWorker.new
Signal.trap('INT') { worker.stop }
Signal.trap('TERM') { worker.stop }
worker.run
