Hi,

We received a password recovery request for the following vpsAdmin account<%= @accounts.length == 1 ? '' : 's' %>:

<% @accounts.each do |account| -%>
Login: <%= account[:login] %>
<% if account[:outcome] == 'recoverable' -%>
Set a new password: <%= account[:reset_url] %>

This link is valid for one hour. After opening it, you will need to verify your
identity with TOTP or a passkey.
<% elsif account[:outcome] == 'administrator' -%>
Password recovery is not available for administrator accounts. Ask another
administrator to change the password.
<% elsif account[:outcome] == 'no_mfa' -%>
This account does not have two-factor authentication configured, so its
password cannot be recovered automatically. Please contact <%= @support_mail %>.
<% else -%>
Password recovery is not available for this account. Please contact
<%= @support_mail %> if you need help.
<% end -%>

<% end -%>
If you did not request this email, you can ignore it. No password has been
changed.

Best regards,

vpsAdmin team

(This is an automated mail from vpsAdmin, your reply will be sent to our support)
