Hello <%= @user.login %>,

The password for your vpsAdmin account was changed.

Time: <%= local_time(@time, '%Y-%m-%d %H:%M %Z') %>
IP address: <%= @request.env['HTTP_CLIENT_IP'] || @request.env['HTTP_X_REAL_IP'] || @request.ip %>
User agent: <%= @request.user_agent || 'unknown' %>

If this was you, you can ignore this message. Otherwise contact
<%= @support_mail %> as soon as possible.

Best regards,

vpsAdmin team

(This is an automated mail from vpsAdmin, your reply will be sent to our support)
