VPS #<%= @vps.id %> <%= @vps.hostname %>: <%= @all_oom_count %> out-of-memory events
<% @selected_oom_reports.each do |report| -%>
- <%= local_time(report.created_at, '%Y-%m-%d %H:%M %Z') %>: killed <%= report.killed_name || 'nothing' %>, <%= report.count %>x
<% end -%>
<% url = webui_url("?page=oom_reports&vps=#{@vps.id}&list=1") -%>
<% if url.present? -%>

Open: <%= url %>
<% end -%>
