VPS #<%= @vps.id %> <%= @vps.hostname %>: <%= @all_oom_count %> OOM
<% @selected_oom_reports.first(3).each do |report| -%>
<%= report.killed_name || 'nic neukončeno' %>: <%= report.count %>x
<% end -%>
