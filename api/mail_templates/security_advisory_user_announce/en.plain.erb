Hi <%= @user.login %>,

we would like to inform you about a security advisory for vpsAdmin infrastructure.
The vulnerability has already been mitigated on affected nodes.

Advisory: #<%= @a.id %><%= @a.name ? " - #{@a.name}" : '' %>
CVEs: <%= @a.cves %>
Published at: <%= local_time(@a.published_at, '%Y-%m-%d %H:%M %Z') %>

Your affected VPS:

<% @vpses.each do |vps| -%>
  - VPS #<%= vps.vps_id %> - <%= vps.vps.hostname %> (on <%= vps.node.domain_name %>)
    Vulnerable until: <%= local_time(vps.vulnerable_until, '%Y-%m-%d %H:%M %Z') %>
    Mitigated since: <%= local_time(vps.mitigated_since, '%Y-%m-%d %H:%M %Z') %>
<% end -%>

Summary:
<%= @a.en_summary %>

Response:
<%= @a.en_response %>

More information:
<%= @webui_url %>/?page=security_advisory&action=show&id=<%= @a.id %>

Best regards,

vpsAdmin team

(This is an automated mail from vpsAdmin, your reply will be sent to our support)
