Hello <%= @vps.user.login %>,

vpsAdmin recorded out-of-memory events on VPS #<%= @vps.id %> (<%= @vps.hostname %>).

Selected events: <%= @selected_oom_count %> of <%= @all_oom_count %>

<% @selected_oom_reports.each do |report| -%>
- <%= report.created_at.localtime.strftime('%Y-%m-%d %H:%M %Z') %>: invoked by <%= report.invoked_by_name %>[<%= report.invoked_by_pid %>], killed <%= report.killed_name || 'unknown' %><%= report.killed_pid ? "[#{report.killed_pid}]" : '' %>, count <%= report.count %>
<% end -%>
<% if @all_oom_count > @selected_oom_count -%>

There are <%= @all_oom_count - @selected_oom_count %> additional events not shown here.
<% end -%>
<% if @base_url && !@base_url.empty? -%>

vpsAdmin: <%= @base_url %>
<% end -%>

Best regards,

vpsAdmin
