Hello <%= @user.login %>,

vpsAdmin monitoring has closed the zombie process alert for VPS #<%= @vps.id %> (<%= @vps.hostname %>).

Current count: <%= @zombie_process_count %>
Threshold: <%= @threshold %>
<% if @base_url && !@base_url.empty? -%>
vpsAdmin: <%= @base_url %>
<% end -%>

Best regards,

vpsAdmin
